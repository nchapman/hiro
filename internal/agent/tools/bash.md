Execute a shell command.

Synchronous commands auto-background after 60s. Output truncated at 32KB. Use `run_in_background` for long-running processes; use `job_output` and `job_kill` to manage them.
