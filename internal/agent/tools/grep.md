Search file contents by regex pattern. Max 100 matches. Use `literal_text` for exact string matching. Use `include` to filter by file type (e.g. `*.go`). Skips hidden files, node_modules, vendor, dist, .git.
