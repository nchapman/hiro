List files and directories at a given path.

Automatically skips hidden directories, node_modules, vendor, dist, and .git. Results are limited to 500 entries.

Use this to explore project structure, find files, or understand directory layout.
