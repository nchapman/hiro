Read the contents of a file, displayed with line numbers.

Use this tool to examine source code, configuration files, logs, or any text file. Supports reading specific line ranges with offset and limit parameters for large files.

## Guidelines

- Always read a file before attempting to modify it.
- For large files, use offset and limit to read specific sections.
- Line numbers are 1-based.
