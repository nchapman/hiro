Remove a memory by matching against existing entries. Provide a substring that uniquely identifies the memory to forget.

- The substring is matched case-insensitively against all memory entries.
- If multiple entries match, all matches are removed.
- If no entries match, an error is returned.
