Permanently delete an instance and all its data. Cannot be undone.

- Removes the instance, its sessions, memory, todos, and conversation history
- Also deletes all descendant instances
- Scoped to your descendants