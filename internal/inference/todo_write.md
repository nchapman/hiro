Replace your task list. Send the complete list each time — omitted items are removed.

- Statuses: `pending`, `in_progress`, `completed`
- Use `active_form` for a present-continuous label shown while in progress (e.g., "Running tests")
- Exactly one task should be `in_progress` at a time

Best practices:
- Use for multi-step tasks (3+ steps) to track progress and show the user what you're doing
- Mark a task `in_progress` before starting work on it
- Mark tasks `completed` as you finish them — don't batch completions
- Only mark a task completed when it's fully done — not if there are errors or blockers remaining
- Skip this tool for single, trivial tasks — just do the work directly