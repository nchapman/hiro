Load a skill's full instructions by name.

- Only name and description appear in your system prompt — call this tool to get the full instructions
- Returns the skill body and lists any bundled resources (scripts, references, assets)

Best practices:
- Call this tool before performing a skill-matched task, not after
- If a skill bundles resources, use Read to access them by path