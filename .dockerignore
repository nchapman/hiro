.git
.env
sessions/
web/ui/node_modules
web/ui/dist

# Root-level docs (keep internal/**/*.md for go:embed)
/*.md

# Runtime state — seeded by the binary at startup, not baked in
agents/
config/
db/
instances/
skills/
workspace/

# Secrets
*.pem
*.key
*.crt
.ssh/
.aws/
