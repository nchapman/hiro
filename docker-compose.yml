services:
  hiro:
    image: ghcr.io/nchapman/hiro:latest
    restart: unless-stopped
    init: true
    ports:
      - "8120:8120"
    volumes:
      - hiro:/home/hiro
      # Or use a host directory instead of the named volume for easy access
      # to agent state, config, and workspace from your host filesystem:
      #   - /Users/you/hiro:/home/hiro
      # Expose host directories to agents by bind-mounting them under
      # /home/hiro/mounts/<name>. Add `:ro` for read-only access.
      # Example:
      #   - /Users/you/Photos:/home/hiro/mounts/photos:ro
      #   - /Users/you/scratch:/home/hiro/mounts/scratch
    labels:
      - "com.centurylinklabs.watchtower.enable=true"

  watchtower:
    image: containrrr/watchtower
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --label-enable --cleanup

volumes:
  hiro:
