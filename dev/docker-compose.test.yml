# Unified test runner.
# Used by: make test, make test-isolation, make test-netiso, make check
services:
  test:
    build:
      context: ..
      target: test
    init: true
    command: ["go", "test", "-tags", "sqlite_fts5", "-race", "./...", "-v", "-count=1"]
